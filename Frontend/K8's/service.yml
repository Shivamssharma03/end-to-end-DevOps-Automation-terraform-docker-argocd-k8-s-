apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: default
spec:
  selector:
    app: frontend
  type: LoadBalancer            # Gives Public IP on EKS
  ports:
    - port: 80
      targetPort: 80
